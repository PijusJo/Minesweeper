<UserControl x:Class="Minesweeper.View.UserControls.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UserControls="clr-namespace:Minesweeper.View.UserControls"
             xmlns:ViewModel="clr-namespace:Minesweeper.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid Margin="20">
        <ItemsControl ItemsSource="{Binding FlattenedCells}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Rows="{Binding Rows}" Columns="{Binding Columns}" Background="White"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type ViewModel:LocationViewModel}">
                    <UserControls:LocationView/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            
            
            
            
            
        </ItemsControl>
    </Grid>
    
</UserControl>
