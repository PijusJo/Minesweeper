<UserControl x:Class="Minesweeper.View.UserControls.LocationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Minesweeper.View.UserControls"
             mc:Ignorable="d" 
             Height="40" Width="40">
    <Grid Background="White" >
        <Border BorderBrush="Black" BorderThickness="1"/>
        <TextBlock Text="{Binding Text}" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="20"/>
        <Button Background="{Binding Colour}" Margin="2" Opacity="{Binding Opacity}" >
            <Button.InputBindings>
                <MouseBinding MouseAction="LeftClick" Command="{Binding LeftClickCommand}"/>
                <MouseBinding MouseAction="RightClick" Command="{Binding RightClickCommand}"/>
                
            </Button.InputBindings>
            <Image Source="/Images/Flag.png" Opacity="{Binding FlagOpacity}" Margin="-1"/>
        </Button>
        
    </Grid>

</UserControl>
